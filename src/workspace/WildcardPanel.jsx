import React from 'react'
export default function WildcardPanel({ input, setInput, draft, onParse, onConfirm, canConfirm }) {
  return <section style={{ maxWidth: 760, margin: '0 auto 20px', padding: 16, borderRadius: 12, background: '#171a29', border: '1px solid #433b59' }}><div style={{ fontSize: 12, color: '#c4b5fd', marginBottom: 8 }}>WILDCARD · describe a custom action</div><div style={{ display: 'flex', gap: 8 }}><input value={input} onChange={(event) => setInput(event.target.value)} placeholder="e.g. slow touch on chest" maxLength={240} style={{ flex: 1, padding: 10, borderRadius: 8 }} /><button onClick={onParse} style={{ padding: '9px 12px', borderRadius: 8, cursor: 'pointer' }}>Parse</button></div>{draft && <div style={{ marginTop: 10, color: draft.accepted ? '#d9fbe9' : '#f7b6b6', fontSize: 13 }}>{draft.accepted ? `Parsed · confidence ${Math.round(draft.confidence * 100)}% · confirm to execute` : draft.reason}<button onClick={onConfirm} disabled={!draft.accepted || !canConfirm} style={{ marginLeft: 10, padding: '5px 9px', borderRadius: 7, cursor: 'pointer' }}>Confirm</button></div>}</section>
}
