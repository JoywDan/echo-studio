import React from 'react'
const allZones = ['lips', 'ears', 'neck', 'shoulders', 'chest', 'abdomen', 'lower_back', 'buttocks', 'inner_thighs', 'perineum', 'penis', 'hands', 'feet']
const techniques = ['touch', 'stroke', 'press', 'pause', 'withdraw', 'command']
export default function JoyPlaybook({ playbook, onChange }) {
  const toggle = (key, value) => { const values = playbook[key]; onChange({ ...playbook, [key]: values.includes(value) ? values.filter((item) => item !== value) : [...values, value] }) }
  return <details style={{ maxWidth: 900, margin: '12px auto 20px', padding: 14, borderRadius: 12, background: '#201a2d', border: '1px solid #4b3d62' }}><summary style={{ cursor: 'pointer', color: '#d8b4fe' }}>JOY'S PLAYBOOK · encrypted with the session</summary><div style={{ marginTop: 14 }}><div style={{ fontSize: 12, color: '#aaa0b7' }}>Preferred zones</div><div style={{ display: 'flex', flexWrap: 'wrap', gap: 6, margin: '8px 0 14px' }}>{allZones.map((zone) => <button key={zone} onClick={() => toggle('preferredZones', zone)} style={{ padding: '6px 9px', borderRadius: 8, color: '#fff', background: playbook.preferredZones.includes(zone) ? '#6d3fa0' : '#312b3c', cursor: 'pointer' }}>{zone}</button>)}</div><div style={{ fontSize: 12, color: '#aaa0b7' }}>Avoided techniques</div><div style={{ display: 'flex', flexWrap: 'wrap', gap: 6, margin: '8px 0 14px' }}>{techniques.map((technique) => <button key={technique} onClick={() => toggle('avoidedTechniques', technique)} style={{ padding: '6px 9px', borderRadius: 8, color: '#fff', background: playbook.avoidedTechniques.includes(technique) ? '#8f304f' : '#312b3c', cursor: 'pointer' }}>{technique}</button>)}</div><label style={{ display: 'block', fontSize: 12, color: '#aaa0b7' }}>Intensity ceiling · {playbook.intensityCeiling}<input type="range" min="20" max="100" value={playbook.intensityCeiling} onChange={(event) => onChange({ ...playbook, intensityCeiling: Number(event.target.value) })} style={{ width: '100%', marginTop: 7 }} /></label><textarea value={playbook.notes} onChange={(event) => onChange({ ...playbook, notes: event.target.value.slice(0, 500) })} placeholder="Private notes (saved only inside encrypted Vault)" style={{ width: '100%', minHeight: 70, marginTop: 12, padding: 9, borderRadius: 8 }} /></div></details>
}
